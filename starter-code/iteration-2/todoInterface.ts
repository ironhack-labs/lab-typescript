// Write the interface for class Todo. It must have:
export interface InterMain {
  ArrString: Array<string>;
  addTask(string):number;
  listAllTasks();
  deleteTask(string):number;
}
// - An array of strings as a property.

//export InterMain;
